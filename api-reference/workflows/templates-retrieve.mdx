---
title: "Retrieve a template"
description: "GET /v1/workflows/templates/{slug}"
openapi: "openapi.json GET /v1/workflows/templates/{slug}"
---

The full template, including its `definition` and a `sample_input` you can run it with unchanged.
